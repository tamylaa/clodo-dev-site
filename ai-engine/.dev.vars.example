# ═══════════════════════════════════════════════════════════════════════
# AI Engine — Local Development Secrets
# ═══════════════════════════════════════════════════════════════════════
# Copy this file to .dev.vars and fill in your actual keys.
# .dev.vars is gitignored — NEVER commit real secrets.
#
# For Cloudflare Workers, `wrangler dev` reads .dev.vars automatically.
# For production, use: wrangler secret put <KEY_NAME>
# ═══════════════════════════════════════════════════════════════════════

# ── Authentication ────────────────────────────────────────────────────
# Shared token for visibility-analytics → ai-engine calls
AI_ENGINE_TOKEN=your-secure-token-here

# ── Provider API Keys (at minimum configure Claude) ───────────────────

# Anthropic Claude (PREFERRED — best quality for SEO analysis)
# Get your key: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here

# OpenAI (GPT-4o, GPT-4-turbo, o1, Codex)
# Get your key: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-key-here

# Google Gemini (Gemini 2.0 Flash, Gemini 1.5 Pro)
# Get your key: https://aistudio.google.com/app/apikey
GOOGLE_AI_API_KEY=your-google-ai-key-here

# Mistral AI (Mistral Large, Codestral)
# Get your key: https://console.mistral.ai/api-keys
MISTRAL_API_KEY=your-mistral-key-here

# DeepSeek (DeepSeek-V3, DeepSeek-R1 — excellent value)
# Get your key: https://platform.deepseek.com/api_keys
DEEPSEEK_API_KEY=your-deepseek-key-here

# ── Cloudflare Workers AI ────────────────────────────────────────────
# Workers AI is available automatically via env.AI binding — no API key needed.
# It's free tier: 10,000 neurons/day. Used as the fallback provider.

# ── Provider Preference ──────────────────────────────────────────────
# Controls which provider is used by default
# Options: auto | claude | openai | gemini | mistral | deepseek | cloudflare
# "auto" = smart routing based on task complexity (Claude for complex, CF AI for simple)
AI_PROVIDER=auto

# ── Model Overrides (optional) ───────────────────────────────────────
# Override default model selection per provider
# CLAUDE_MODEL=claude-sonnet-4-20250514
# OPENAI_MODEL=gpt-4o
# GEMINI_MODEL=gemini-2.0-flash
# MISTRAL_MODEL=mistral-large-latest
